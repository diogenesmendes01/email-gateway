#!/usr/bin/env sh

echo "🤖 Validando commit..."

# Verificar se arquivos de ambiente de PRODUÇÃO estão sendo commitados
if git diff --cached --name-only | grep -qE "\.env\.(production|prod|staging)"; then
  echo "❌ ERRO: Tentativa de commitar arquivo de ambiente de PRODUÇÃO"
  echo "Arquivos .env.production, .env.prod e .env.staging NÃO devem ser commitados"
  echo "Use .env.development ou .env.local para configurações de desenvolvimento"
  exit 1
fi

# Verificar se .env.production.local está sendo commitado
if git diff --cached --name-only | grep -q "\.env\.production\.local"; then
  echo "❌ ERRO: Tentativa de commitar arquivo .env.production.local"
  echo "Arquivos de produção NÃO devem ser commitados"
  exit 1
fi

# PERMITIR: .env.development, .env.local, .env.test (são seguros para commit)
# Mensagem informativa caso esteja commitando arquivos .env permitidos
if git diff --cached --name-only | grep -qE "\.env\.(development|local|test)"; then
  echo "ℹ️  Commitando arquivo de configuração de desenvolvimento (permitido)"
fi

# Verificar se há arquivos de credenciais
if git diff --cached --name-only | grep -qE "(credentials|secrets|\.key|\.pem)"; then
  echo "⚠️  AVISO: Arquivos que podem conter credenciais detectados"
  echo "Verifique se não há informações sensíveis sendo commitadas"
  # No Windows, não podemos fazer prompt interativo facilmente
  # então apenas avisamos
fi

# Verificar se node_modules está sendo commitado
if git diff --cached --name-only | grep -q "node_modules"; then
  echo "❌ ERRO: Tentativa de commitar node_modules/"
  echo "node_modules/ deve estar no .gitignore"
  exit 1
fi

# Verificar se dist está sendo commitado
if git diff --cached --name-only | grep -q "dist/"; then
  echo "❌ ERRO: Tentativa de commitar arquivos de build (dist/)"
  echo "Arquivos de build devem estar no .gitignore"
  exit 1
fi

echo "✅ Validações de segurança passaram"
